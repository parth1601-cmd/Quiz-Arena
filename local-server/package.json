{
  "name": "quiz-arena-local-server",
  "version": "1.0.0",
  "private": true,
  "description": "Runs entirely on the local machine/LAN — no internet or AWS required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
