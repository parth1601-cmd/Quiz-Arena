{
  "name": "backend",
  "version": "1.0.0",
  "description": "Structure follows a function-per-domain layout, one folder per API resource area. Implemented incrementally starting Phase 2 (auth).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1112.0",
    "@aws-sdk/client-dynamodb": "^3.1112.0",
    "@aws-sdk/lib-dynamodb": "^3.1112.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  }
}
