{
  "name": "infrastructure",
  "version": "1.0.0",
  "description": "Provisions Cognito, API Gateway, Lambda, DynamoDB, WebSocket API, S3, CloudFront, IAM, CloudWatch. Stacks are added incrementally as each phase introduces new resources — Phase 1 has no live infrastructure yet, since there's nothing to deploy until Phase 2 (Cognito).",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-cdk-lib": "^2.265.0",
    "constructs": "^10.8.1",
    "source-map-support": "^0.5.21",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/uuid": "^10.0.0",
    "aws-cdk": "^2.1136.0",
    "esbuild": "^0.28.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
